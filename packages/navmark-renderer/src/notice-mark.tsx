import type { Tags } from './util/types.def.js';

export async function renderNoticeMark(
  tags: Tags,
): Promise<ImageData | undefined> {
  console.info('notice!', tags);
  // TODO: implement this
  return undefined;
}
