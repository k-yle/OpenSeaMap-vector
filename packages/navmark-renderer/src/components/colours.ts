/**
 * some named HTML colours are ugly or different to what
 * a nautical chart would normally show. So we can any
 * such colours here.
 */
export const COLOUR_OVERRIDES: Record<string, string> = {
  yellow: '#fc0',
  orange: '#f80',
};
